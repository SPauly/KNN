//#pragma once
//#include "neuron.h"
//
//class XORNet {
//	size_t data_size = 2;
//
//	unique_ptr<float[]> data{ (make_unique<float[]>(data_size)) };
//	unique_ptr<float[]> weights{ (make_unique<float[]>(data_size)) };
//
//	unique_ptr<float[]> data1{ (make_unique<float[]>(data_size + 1)) };
//	unique_ptr<float[]> weights1{ (make_unique<float[]>(data_size + 1)) };
//
//public:
//	XORNet() = delete;
//	XORNet(float, float, float, float);
//	~XORNet();
//};
